[project]
name = "shadowquic-docs"
version = "0.1.0"
description = "MkDocs site generator for shadowquic's configuration reference"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "pymdown-extensions>=10.7",
]
