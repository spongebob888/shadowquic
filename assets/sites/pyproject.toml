[project]
name = "shadowquic-docs"
version = "0.1.0"
description = "Zensical site generator for shadowquic's configuration reference"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "zensical>=0.0.43",
]
